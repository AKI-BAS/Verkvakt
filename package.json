{
  "name": "verkvakt-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "run-now": "wrangler dev --test-scheduled",
    "rescore": "node scripts/rescore.mjs",
    "rescore:apply": "node scripts/rescore.mjs --apply"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
